name:
slug:          # used for dir names, wallpaper folder, etc.

concept: >

feeling: >

# ---------------------------------------------------------------------------
# RAW PALETTE -- every named color, its hex, and a small note about its usage.
# ---------------------------------------------------------------------------
palette:


# one-off colors that don't fit the named-swatch pattern
extra:
  autosuggestions:
  mist_text:
  border_soft:
  border_strong:

# ---------------------------------------------------------------------------
# SEMANTIC ROLES -- every generator/template should reference THESE keys,
# never raw palette names directly. Values are palette/derived/extra keys,
# NOT hex codes, so re-theming = editing palette/derived above only.
# ---------------------------------------------------------------------------
semantic:
  background:
  surface:
  alt_surface:

  active_border:
  border_inactive_soft:
  border_hover_strong:

  hypr_border_active:
  hypr_border_inactive:
  hypr_shadow:

  text_primary:
  text_secondary:
  cursor:
  fish_commands:
  fish_keywords:
  fish_strings:
  fish_comments:
  
  operators:
  escape_sequences:
  autosuggestions:
  success:
  warning:
  error:
  
  hover:
  selected:
  url:
  
  reading_text_secondary:

  info:
  surface_mid:
  muted_text:
  ember_warm:
  starship_green:

glow:
  color:
  alpha:

# ---------------------------------------------------------------------------
# ANSI 16-color terminal palette
# ---------------------------------------------------------------------------
ansi:
  - { index: 0,  name: black,          hex: "" }
  - { index: 1,  name: red,            hex: "" }
  - { index: 2,  name: green,          hex: "" }
  - { index: 3,  name: yellow,         hex: "" }
  - { index: 4,  name: blue,           hex: "" }
  - { index: 5,  name: magenta,        hex: "" }
  - { index: 6,  name: cyan,           hex: "" }
  - { index: 7,  name: white,          hex: "" }
  - { index: 8,  name: bright_black,   hex: "" }
  - { index: 9,  name: bright_red,     hex: "" }
  - { index: 10, name: bright_green,   hex: "" }
  - { index: 11, name: bright_yellow,  hex: "" }
  - { index: 12, name: bright_blue,    hex: "" }
  - { index: 13, name: bright_magenta, hex: "" }
  - { index: 14, name: bright_cyan,    hex: "" }
  - { index: 15, name: bright_white,   hex: "" }

# ---------------------------------------------------------------------------
# Transparency & blur
# ---------------------------------------------------------------------------
transparency:
  swaync_center:     { alpha: }
  hyprlock_bg:       { brightness: , blur_passes: , blur_size: }

# ---------------------------------------------------------------------------
# Typography
# ---------------------------------------------------------------------------
typography:
  hyprlock:  "Iosevka Nerd Font Propo"

# ---------------------------------------------------------------------------
# Borders & rounding
# ---------------------------------------------------------------------------
borders:
  kitty:             { active: , inactive: }

text:
  hyprlock_text:

# ---------------------------------------------------------------------------
# Key glyphs (Nerd Font)
# ---------------------------------------------------------------------------
glyphs:
  prompt_success: ""
  prompt_error:   ""
  vim_normal:     ""
  git_branch:     "󰘬"
  dir_prefix:     ""
  home:           "󱂵"
  flavor_star:    "✦"
  os:             "󰣇"

fastfetch_logo: dragon.txt

# ---------------------------------------------------------------------------
# Freeform notes about anything needing extra comments.
# ---------------------------------------------------------------------------
notes:
  readability: >

  rules:
    - "pure black (#000000) and pure white (#ffffff) are never used"
